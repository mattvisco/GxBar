/**
 * Created by M on 11/18/15.
 */

// TODO: onboarding code should enable extension, then inform the user to drag element, on each event have an onboarding check
var startOnboarding = function() {
    onboarding = true; // Make this global
    enableExtension(true);
};